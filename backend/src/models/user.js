const bcrypt = require('bcrypt'); module.exports = (sequelize, DataTypes) => { const User = sequelize.define('User',{ id:{type:DataTypes.INTEGER,primaryKey:true,autoIncrement:true}, name:DataTypes.STRING, email:{type:DataTypes.STRING,unique:true,allowNull:false}, password:{type:DataTypes.STRING,allowNull:false} },{timestamps:true}); User.beforeCreate(async u=>{ u.password = await bcrypt.hash(u.password,10); }); User.prototype.validatePassword = function(p){ return bcrypt.compare(p,this.password); }; return User; };
