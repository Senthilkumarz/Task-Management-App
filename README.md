Fullstack Task Manager - scaffold
Run backend: cd backend && npm install && cp .env.example .env && npm run dev
Run frontend: cd frontend && npm install && npm start
Or docker-compose up --build
"# Task-Management-App" 
"# Task-Management-App" 
